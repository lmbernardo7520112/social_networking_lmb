<div style="height: 150%!important;background-image: url('<%= image_path('backgroundSalaAula_2.png') %>'); background-repeat: no-repeat; background-size: cover">
  <div class="offset-4 col-4 authentication-inner py-5">
    <div class="card" style="background-color: hsla(201, 100%, 6%, 0.6%);">
      <div class="p-4 p-sm-5">
        <div class="d-flex justify-content-center align-items-center pb-2 mb-4">
          <%= image_tag('logoEscola', style: 'height: 90px;') %>
        </div>

        <% if alert %>
          <div class="d-flex justify-content-center">
            <p class="text-danger"><%= alert %></p>
          </div>
        <% end %>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <% render 'layouts/form_errors', record: resource %>
          <div class="form-group">
          <div class="form-group">
            <%= f.label :name, class: 'form-label' %>
            <%= f.text_field :name, autofocus: true, autocomplete: "name", class:" form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :phone, class: 'form-label' %>
            <%= f.text_field :phone, autofocus: true, autocomplete: "phone", class:" form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :avatar, class: 'form-label' %>
            <%= f.file_field (:avatar) %>
          </div>
            <%= f.label :email, class: 'form-label' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class:" form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :password, class: 'form-label' %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
          </div>
          <div class="d-flex justify-content-center align-items-center m-0">
            <%= f.submit 'Cadastrar', class: "btn btn-primary" %>
          </div>
        <% end %>

          <%= link_to new_user_session_path, class: "d-flex justify-content-center align_items-center mt-4" do %>
            Login
          <% end %>
      </div>
    </div>
  </div>
</div>